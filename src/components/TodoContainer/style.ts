import styled from 'styled-components'

export const TodoContainerBlock = styled.div`
  width: 512px;
  height: 768px;
  position: relative;
  background: white;
  border-radius: 1.6rem;
  box-shadow: 0 0 8px 0 rgba(0, 0, 0, 0.04);
  margin: 0 auto;
  margin-top: 9.6rem;
  margin-bottom: 3rem;
  display: flex;
  flex-direction: column;
`
